- error = @dtweet.errors.include?(:user)
fieldset class='control-group#{error ? ' has-error' : ''}'
  = f.label :user, :class => 'control-label'
  div class='controls'
    = f.text_field :user, :class => 'form-control input-large input-with-feedback', :autofocus => true
    span class='help-inline' = error ? f.error_message_on(:user, :class => 'text-error') : pat(:example)
- error = @dtweet.errors.include?(:tweetId)
fieldset class='control-group#{error ? ' has-error' : ''}'
  = f.label :tweetId, :class => 'control-label'
  div class='controls'
    = f.text_field :tweetId, :class => 'form-control input-large input-with-feedback'
    span class='help-inline' = error ? f.error_message_on(:tweetId, :class => 'text-error') : pat(:example)
- error = @dtweet.errors.include?(:body)
fieldset class='control-group#{error ? ' has-error' : ''}'
  = f.label :body, :class => 'control-label'
  div class='controls'
    = f.text_area :body, :class => 'form-control input-large input-with-feedback'
    span class='help-inline' = error ? f.error_message_on(:body, :class => 'text-error') : pat(:example)
- error = @dtweet.errors.include?(:turnId)
fieldset class='control-group#{error ? ' has-error' : ''}'
  = f.label :turnId, :class => 'control-label'
  div class='controls'
    = f.text_field :turnId, :class => 'form-control input-large input-with-feedback'
    span class='help-inline' = error ? f.error_message_on(:turnId, :class => 'text-error') : pat(:example)
- error = @dtweet.errors.include?(:user_id)
fieldset class='control-group#{error ? ' has-error' : ''}'
  = f.label :user_id, :class => 'control-label'
  div class='controls'
    = f.text_field :user_id, :class => 'form-control input-large input-with-feedback'
    span class='help-inline' = error ? f.error_message_on(:user_id, :class => 'text-error') : pat(:example)

div class="form-actions"
  = f.submit pat(:save), :class => 'btn btn-primary'
  |&nbsp;
  = f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  |&nbsp;
  = link_to pat(:cancel), url(:dtweets, :index), :class => 'btn btn-default'